{
  "name": "proxy-imed",
  "version": "1.0.0",
  "description": "Proxy para acceder a iMed con Puppeteer",
  "main": "proxy-imed.js",
  "scripts": {
    "start":" PUPPETEER_CACHE_DIR=/tmp/puppeteer npx puppeteer browsers install chromium && PUPPETEER_CACHE_DIR=/tmp/puppeteer node proxy-imed.js,    "dev": "node proxy-imed.js"
  },
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer": "^21.0.0"
  }
}
